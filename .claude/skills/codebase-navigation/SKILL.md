---
name: codebase-navigation
description: Locate the smallest set of files and symbols needed in an unfamiliar or large codebase — "where is X handled", "how does Y work here", "what calls Z", "which file owns this". Prevents whole-repository scanning and context waste.
---

# Codebase Navigation

Read `.ai/skills/codebase-navigation.md` now and follow it. It holds the full method, the
constraints, the efficiency guidance, and the output you owe.

This file is an adapter. It adds no rules.
