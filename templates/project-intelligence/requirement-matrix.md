# Requirement Matrix

Every requirement as one line, with the citation that defines it and a hint a test could check. A
requirement stated in two documents is one row with two citations. Procedure:
`.ai/workflows/ingest-project.md` §6.

- Updated: [YYYY-MM-DD]

| ID | Requirement | Module | Priority | Source | Acceptance hint | Status |
| --- | --- | --- | --- | --- | --- | --- |
| R-001 | [A user can export invoices for a date range] | [Invoicing] | [must] | [S-01 §4.3] | [An export of a known range returns exactly its invoices] | [not started] |

Priority: `must` · `should` · `could`

Status: `not started` · `in progress` · `done` · `disputed` — a `disputed` row has a matching entry
in the open-decisions map.
